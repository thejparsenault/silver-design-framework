# Production recipe

The bundled recipe is static HTML, semantic CSS, and minimal JavaScript.
Production output must be rebuilt from accepted design intent, never silently
copied from prototype code, and must pass all production checks.
