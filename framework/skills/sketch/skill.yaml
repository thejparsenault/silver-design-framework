schema: silver/skill/v2
id: sketch
version: 0.7.0
summary: Create inexpensive, constraint-aware design alternatives.
context_budget: small
inputs:
  - kind: concept
    required: false
    role: working
  - kind: design-specification
    required: false
    role: working
  - kind: flow
    required: false
    role: working
  - kind: prototype
    required: false
    role: working
outputs:
  - kind: sketch
    scope: product
    path_pattern: design/work/sketches/**
    authority: project-owned
capabilities:
  required:
    - repository
    - sketch-renderer
  optional:
    - capability: design-file
      providers: []
      fallback: degraded
      description: Use design-file when configured; preserve explicit fallback
        coverage otherwise.
effects:
  - capability: repository
    actions:
      - read
      - inspect
    paths:
      - design/**
      - prototypes/**
      - presentations/**
      - production/**
      - reference-system/**
      - .silver/**
  - capability: repository
    actions:
      - create
      - write
      - update
    paths:
      - design/work/sketches/**
guardrails:
  - authority-respected
  - effects-declared
  - no-silent-mutation
  - revision-pinned
  - followups-recommended-only
  - semantic-styles-only
  - constraint-profile-explicit
completion:
  invariants:
    - fidelity-declared
    - alternatives-cheap
    - constraint-profile-honored
  quality_criteria:
    - id: task-quality
      description: Sketches make consequential structure and behavior choices
        reviewable without false production fidelity.
      evaluation: human
  unresolved_questions: block-handoff
  review:
    required: true
    reviewer: human
    checkpoint: Review and accept the result before a readiness-gated handoff.
checks:
  - id: contract-integrity
    required: true
    blocks: &a1
      - evaluate
      - prototype
      - component
  - id: semantic-styles
    required: true
    blocks: *a1
  - id: accessibility
    required: true
    blocks: *a1
handoffs:
  - id: sketch-to-evaluate
    target: evaluate
    readiness: evaluate
    requires:
      - sketch
  - id: sketch-to-prototype
    target: prototype
    readiness: prototype
    requires:
      - sketch
  - id: sketch-to-component
    target: component
    readiness: component
    requires:
      - sketch
external_effects:
  - none
scripts:
  - id: invoke
    path: scripts/invoke.mjs
    purpose: Run this skill through the shared guarded invocation runtime.
  - id: render-sketch
    path: scripts/render-sketch.mjs
    purpose: Bundled deterministic sketch operation.
recommend_after: *a1
