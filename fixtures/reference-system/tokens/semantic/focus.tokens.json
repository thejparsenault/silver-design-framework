{
  "focus": {
    "$description": "Focus ring tokens. Applied to all interactive elements via :focus-visible. Must never be removed — only styled.",
    "ring-color": {
      "$type": "color",
      "$description": "Focus outline color",
      "$value": "{color.blue.450}"
    },
    "ring-offset-color": {
      "$type": "color",
      "$description": "Focus outline offset gap color — typically matches the surface behind the element",
      "$value": "{color.neutral.0}"
    },
    "ring-width": {
      "$type": "dimension",
      "$description": "Focus outline stroke width",
      "$value": { "value": 2, "unit": "px" }
    },
    "ring-offset": {
      "$type": "dimension",
      "$description": "Gap between element edge and focus ring",
      "$value": { "value": 2, "unit": "px" }
    }
  }
}
